{% extends 'base.html.twig'|extendable(event_type) %}

{% block head %}
    {{ getCSS('template1') }}
    {{ getJS('template1', 'prefill') }}
{% endblock %}

{% block content %}
    <h1>{{ lang.header }}</h1>
    <p>{{ lang.description }}</p>
    <hr>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}
